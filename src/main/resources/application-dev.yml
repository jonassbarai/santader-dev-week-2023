spring:
  datasource:
    url: jdbc:h2:mem:sdw2023
    driver-class-name: org.h2.Driver
    username: jonas
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
